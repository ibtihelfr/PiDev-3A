{% extends 'base.html.twig' %}

{% block title %}Réserver Ticket{% endblock %}

{% block body %}
    <section class="ftco-section bg-light">
        <div class="container">
            <div class="row justify-content-center mb-3 pb-3">
                <div class="col-md-12 heading-section text-center ftco-animate">
                    <h2 class="mb-4">Réserver ticket</h2>
                    <h4><a href="{{ path('ticket_index_front') }}">Retour à la liste</a></h4>
                </div>
            </div>
        </div>
        <div class="container">
            {{ include('ticket/formBook.html.twig', {'button_label': 'Reserver'}) }}
        </div>
    </section>
{% endblock %}
