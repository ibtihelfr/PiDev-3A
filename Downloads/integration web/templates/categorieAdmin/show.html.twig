{% extends 'baseBack.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcategorie</th>
                <td>{{ categorie.idcategorie }}</td>
            </tr>
            <tr>
                <th>Nomcategorie</th>
                <td>{{ categorie.nomcategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_back_index') }}">back to list</a>

    <a href="{{ path('app_categorie_back_edit', {'idcategorie': categorie.idcategorie}) }}">edit</a>

    {{ include('categorieAdmin/_delete_form.html.twig') }}
{% endblock %}
