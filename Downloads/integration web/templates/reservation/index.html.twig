{% extends 'baseBack.html.twig' %}

{% block title %}Reservation index{% endblock %}

{% block body %}

<a href="{{ path('app_reservation_stats') }}">Statistiques</a>
           <div class="card">
            <div class="card-body">
                <h5 class="card-title">Basic Datatable</h5>
                <div class="table-responsive">

    <h1>Reservation index</h1>

       <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table id="zero_config" class="table table-striped table-bordered">
                        <thead>
            <tr>
                <th>Idres</th>
                <th>nomEvent</th>
                <th>nomUser</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.idres }}</td>
                <td>{{ reservation.idevent.nomEvent }}</td>
                <td>{{ reservation.idUser.nomUser }}</td>
                <td>
                    <a href="{{ path('app_reservation_show', {'idres': reservation.idres}) }}">Afficher</a>
    {{ include('reservation/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
      </table>
                </div>

            </div>
        </div>        
     <!--table end-->


{% endblock %}
