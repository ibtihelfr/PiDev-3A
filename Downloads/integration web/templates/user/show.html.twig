{% extends 'baseBack.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

    
<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.iduser }}</td>
            </tr>
             <tr>
                <th>nom</th>
                <td>{{ user.nomuser }}</td>
            </tr>
             <tr>
                <th>prenom</th>
                <td>{{ user.prenomuser }}</td>
            </tr>
             <tr>
                <th>pwd</th>
                <td>{{ user.pwd }}</td>
            </tr>
             <tr>
                <th>email</th>
                <td>{{ user.email }}</td>
            </tr>
             <tr>
                <th>numtel</th>
                <td>{{ user.numtel }}</td>
            </tr>
             <tr>
                <th>typeuser</th>
                <td>{{ user.typeuser }}</td>
            </tr>
           
                {% if user.photo %}
    <img src="{{ asset('uploads/' ~ user.photo) }}" alt="{{  'uploads/' ~ user.photo}}" style="max-width: 200px;">
{% else %}
    <p>No image available</p>
{% endif %}
            
        </tbody>
    </table>





   
   

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'iduser': user.iduser}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
     
{% endblock %}