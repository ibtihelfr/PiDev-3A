<form method="post" action="{{ path('app_categorieevent_delete', {'idcat': categorieevent.idcat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorieevent.idcat) }}">
    <button class="btn">Delete</button>
</form>
