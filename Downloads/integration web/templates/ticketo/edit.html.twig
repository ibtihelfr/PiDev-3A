{% extends 'baseBack.html.twig' %}

{% block title %}Edit Ticketo{% endblock %}

{% block body %}
    <section class="ftco-section bg-light">
    <div class="container">
        <div class="row justify-content-center mb-3 pb-3">
            <div class="col-md-12 heading-section text-center ftco-animate">
                <h2 class="mb-4">Modifier ticketO</h2>
                <h4><a href="{{ path('ticketo_index') }}">Retour à la liste</a></h4>
            </div>
        </div>
    </div>
    <div class="container">
        {{ include('ticketo/form.html.twig', {'button_label': 'Modifier'}) }}
        {{ include('ticketo/delete.html.twig') }}
    </div>
    </section>
{% endblock %}
